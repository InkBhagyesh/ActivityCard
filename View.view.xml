<mvc:View
    xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    width="100%"
    controllerName="com.winslow.Activities.Card"
    busyIndicatorDelay="0"
>
    <List
        items="{LocalModel>/}"
        width="auto"
        showSeparators="None"
    >
        <headerToolbar>
            <OverflowToolbar>
                <Title
                    text="ACTIVITY"
                    class="customListTitle"
                    level="H1"
                />
            </OverflowToolbar>
        </headerToolbar>
        <CustomListItem>
            <VBox
                width="auto"
                class="holidayItem sapUiSmallMarginBeginEnd"
            >
                <Text
                    text="{ path: 'LocalModel>date', formatter: '.Formatter.formatTopDate' }"
                    class="holidayDate"
                    wrapping="true"
                />
                <Link
                    class="customListLink"
                    press="handleFilePress"
                    text="{LocalModel>name}"
                    wrapping="true"
                />
                <Text
                    text="{LocalModel>description}"
                    visible="{= !!${LocalModel>description}}"
                    class="holidaySubText"
                    wrapping="true"
                />
                <HBox class="holidayDivider">
                    <VBox class="dividerLine" />
                </HBox>
            </VBox>
            <!-- <HBox
                alignItems="Center"
                renderType="Bare"
            >
                <core:Icon
                    size="2rem"
                    class="sapUiTinyMarginBeginEnd"
                    src="{path: 'LocalModel>mime', formatter: '.Formatter.doctypeicon' }"
                />
                
            </HBox> -->
        </CustomListItem>
        <!-- <items>
            <StandardListItem
                title="{LocalModel>name}"
                description="{LocalModel>description}"
                info="{path: 'LocalModel>date', formatter: '.Formatter.formatDate' }"
                icon="{path: 'LocalModel>mime', formatter: '.Formatter.doctypeicon' }"
                iconInset="false"
                infoTextDirection="Inherit"
                type="Active"
                press=".handleFilePress"
                wrapping="true"
            />
        </items> -->
    </List>
    <Link
        class="sapUiSmallMarginBegin sapUiTinyMargin"
        text="VIEW ALL ACTIVITY"
        press="onPressLink"
        visible="{= ${LocalModel>/}.length > 0 }"
    />
</mvc:View>
